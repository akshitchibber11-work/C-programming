#include <stdio.h>

int main(){
     int length,width,area,perimeter;
     printf("akshit\n");
     printf("enter length and width\n");
     scanf("%d %d",&length, &width);
     area=length*width;
     perimeter=2*(length+width);
     printf("Area will be %d\n",area);
     printf("perimeter will be %d\n",perimeter);
     return 0;
}
